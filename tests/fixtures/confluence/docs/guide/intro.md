# Introduction

Sub-folder pages are nested under parent pages that mirror the directory
structure. So this file becomes a page whose ancestors are `guide` (a folder
page) and the configured root parent.
