name: Setup Tools
description: Setup required tools in the workspace

runs:
  using: composite
  steps:
  - name: Setup Tools
    uses: ./asdf-tools@f77b6cc49f20cbc4c2a592f9dbda9a56ff796636

  - name: Install python tools
    run: |
      pip install -r requirements.txt
      asdf reshim
    shell: bash
